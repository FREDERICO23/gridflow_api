# ─── Application ───────────────────────────────────────────────────────────────
APP_NAME=GridFlow API
APP_VERSION=0.1.0
DEBUG=false

# ─── Authentication ─────────────────────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
API_KEY=your-secret-api-key-here

# ─── Database (TimescaleDB / PostgreSQL) ────────────────────────────────────────
# Matches docker-compose.yml service name "db"
DATABASE_URL=postgresql+asyncpg://gridflow:gridflow@db:5432/gridflow

# ─── Redis (Celery broker + result backend) ─────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ─── Google Cloud Storage ───────────────────────────────────────────────────────
GCS_BUCKET_RAW=gridflow-raw-uploads
GCS_BUCKET_OUTPUT=gridflow-forecast-outputs
# Path to GCS service account JSON inside the container (see docker-compose volumes)
# Leave commented to use Application Default Credentials (ADC) instead
# GCS_CREDENTIALS_PATH=/app/credentials/gcs-service-account.json

# ─── Processing Defaults ────────────────────────────────────────────────────────
# Primary market: Germany
DEFAULT_TIMEZONE=Europe/Berlin
DEFAULT_COUNTRY_CODE=DE

# Max upload file size in bytes (200 MB)
MAX_UPLOAD_SIZE_BYTES=209715200

# Prophet confidence interval — float between 0.50 and 0.99 (default 95%)
FORECAST_CONFIDENCE_INTERVAL=0.95

# Weather enrichment via Open-Meteo (optional — set false to skip)
WEATHER_ENRICHMENT_ENABLED=true

# Include is_imputed flag in forecast download output
INCLUDE_IMPUTED_FLAG_IN_OUTPUT=true
